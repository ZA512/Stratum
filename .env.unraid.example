# ============================================
# Configuration Stratum pour Unraid
# ============================================
# Copier ce fichier en .env et remplir les valeurs

# --- Chemins de données ---
# Où stocker les données persistantes sur Unraid
DATA_PATH=/mnt/user/appdata/stratum

# --- PostgreSQL ---
POSTGRES_USER=stratum
POSTGRES_PASSWORD=CHANGEZ_MOI_mot_de_passe_securise
POSTGRES_DB=stratum
POSTGRES_PORT=5432

# --- Redis ---
REDIS_PORT=6379

# --- Backend ---
BACKEND_PORT=4001
# IMPORTANT: Générer une clé secrète sécurisée (min 32 caractères)
# Exemple: openssl rand -base64 32
JWT_SECRET=CHANGEZ_MOI_cle_secrete_minimum_32_caracteres

# --- Frontend ---
FRONTEND_PORT=3000

# --- CORS (domaines autorisés à accéder à l'API) ---
# Séparer par des virgules si plusieurs domaines
# Exemple: https://stratum.mondomaine.com,https://app.mondomaine.com
CORS_ORIGINS=https://stratum.votredomaine.com

# --- Version des images (optionnel) ---
# Utiliser 'latest' ou un tag spécifique comme 'v1.0.0'
TAG=latest
